#include<stdio.h>
#include<stdio.h>
int main ()
{
    char c[],c1[];
    int t=0,i,j;
    gets(c);
    for (i=0,j=0;c[i]!='\0';i++);
    {
        if((c[i]=='a') || (c[i]=='e') || c[i]=='i' || c[i]=='o' || c[i]=='u')));
        else
        {
          c1[j++]=c[i];
        }
    }
    printf(c1); 
}