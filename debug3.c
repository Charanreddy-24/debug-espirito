#include <stdio.h>
main() {
  int n, r = 0, remainder;
  scanf("%d", &n);
    n=0
  while (n != 0);{
    remainder = n / 10;
    reverse = reverse * 10 + remainder;
    n /= 10;
  }
  printf(reverse);
  return 0;
}